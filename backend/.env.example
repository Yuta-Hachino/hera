# ===================================
# Gemini API設定
# ===================================
# Gemini APIキー（必須）
# 取得方法: https://aistudio.google.com/app/apikey にアクセスして「Create API key」をクリック
GEMINI_API_KEY=your-api-key-here
GEMINI_MODEL=gemini-pro

# ===================================
# Python設定
# ===================================
# Python path for ADK（プロジェクトルートのbackendディレクトリ）
# 例: /path/to/your/project/backend
PYTHONPATH=/path/to/your/project/backend

# ===================================
# セッション管理設定
# ===================================
# セッションデータの保存先ディレクトリ
# - 相対パス: backend/ ディレクトリ基準で解釈される
# - 絶対パス: そのまま使用される
# デフォルト: tmp/user_sessions (backend/tmp/user_sessions)
SESSIONS_DIR=tmp/user_sessions

# カスタマイズ例:
# SESSIONS_DIR=custom_sessions          # backend/custom_sessions
# SESSIONS_DIR=/var/app/sessions        # 絶対パス

# ===================================
# Supabase統合設定（本番環境推奨）
# ===================================
# セッション管理タイプ: file (ローカル開発) | redis (非推奨) | supabase (本番推奨)
SESSION_TYPE=file

# Supabase設定（SESSION_TYPE=supabase の場合に必須）
# 取得方法: Supabaseダッシュボード > Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
SUPABASE_JWT_SECRET=your_jwt_secret_here

# ===================================
# ストレージ管理設定
# ===================================
# ストレージモード: local (ローカル開発) | s3 | gcs | azure | supabase (本番推奨)
STORAGE_MODE=local

# S3設定（STORAGE_MODE=s3 の場合）
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_REGION=us-east-1
# S3_BUCKET_NAME=your_bucket_name

# GCS設定（STORAGE_MODE=gcs の場合）
# GCS_BUCKET_NAME=your_bucket_name
# GCS_PROJECT_ID=your_project_id
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json

# ===================================
# Flask設定
# ===================================
FLASK_DEBUG=False
PORT=8080
ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-url.com

# ===================================
# ロギング設定
# ===================================
LOG_LEVEL=INFO
